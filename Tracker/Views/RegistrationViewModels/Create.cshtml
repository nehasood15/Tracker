@model Tracker.Models.ViewModels.RegistrationViewModel

@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Create</h2>

@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>RegistrationViewModel</h4>
        <hr />
        @foreach ( var item in Model.AllQuestions)
        {
            @Html.DisplayText(item.questionDecription)
            @Html.Label(Model.Responses, htmlAttributes: new { @class = "control-label col-md-2" })

            @Html.DropDownList(response.response, new List<SelectListItem> {
               new SelectListItem {Text = "Yes", Value= "Yes" },
                new SelectListItem {Text = "No", Value= "No" },
           }, new { @class = "form-control" })
                 }

             }
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
